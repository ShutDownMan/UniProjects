#include <stdio.h>

#define MAXROW 256
#define MAXCOL 256

int** readmat(int* nrow, int* ncol, mat[MAXROW][MAXCOL]) {
	
}

int main(int argc, char const *argv[]) {
	int mat[MAXROW][MAXCOL], sumrow[MAXROW], sumcol[MAXCOL];
	int nrow, ncol;

	readmat(&nrow, &ncol, mat);


	return 0;
}